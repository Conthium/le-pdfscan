services:
  - type: web
    name: le-pdf-priority-scanner-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server_scanner:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
